 * page free space optimization
  * free_list maintained as double linkedlist
  * free space map bitmap pages still remain as is
  * page 0 still remains the NULL_PAGE_ID
  * add utility function to insert in free list and remove from free list
  * on freeing
    * fix the free space map bit flag
    * insert it in the free list
  * on allocating
    * allocate from the free list, e only store head
    * if head itself is NULL, extend file and allocate one
  * on truncation
    * check hich pages from the last are free
    * remove it if it is a free space map page
    * or is a free page, also remove it from free list and mark it free
    * finally truncate again to discard the latter section of the file