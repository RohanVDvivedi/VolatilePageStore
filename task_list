 * #define MMAP_PAGE_GROUP_SIZE 1000
 * mmap 1000 pages at once
 * for unmap use a bst and find a pointer lesser than equal to the provided pointer
 * ftrunate updates
   * alvays extend the file by MMAP_PAGE_GROUP_SIZE number of pages
   * and shrink by multiple of MMAP_PAGE_GROUP_SIZE number of pages
 * maintain total_pages_count, along vith active_pages_count
 * all pages betveen them are not in free list and can be directly allocated by setting a bit in its free space map
 * so there vill be a ne allocator mode

 * benchmark sort test case vith 1M (1.3 s old) and 10M (17-18 s old) tuples